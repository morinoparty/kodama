/**
 * Storybook build shim for TanStack Start internal start entry.
 *
 * In the app build, TanStack Start provides a real module for this specifier.
 * Storybook only renders isolated components, so a stub is sufficient.
 */
export default {};
